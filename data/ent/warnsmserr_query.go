// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/predicate"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/warnsmserr"
)

// WarnSmsErrQuery is the builder for querying WarnSmsErr entities.
type WarnSmsErrQuery struct {
	config
	ctx        *QueryContext
	order      []warnsmserr.OrderOption
	inters     []Interceptor
	predicates []predicate.WarnSmsErr
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WarnSmsErrQuery builder.
func (wseq *WarnSmsErrQuery) Where(ps ...predicate.WarnSmsErr) *WarnSmsErrQuery {
	wseq.predicates = append(wseq.predicates, ps...)
	return wseq
}

// Limit the number of records to be returned by this query.
func (wseq *WarnSmsErrQuery) Limit(limit int) *WarnSmsErrQuery {
	wseq.ctx.Limit = &limit
	return wseq
}

// Offset to start from.
func (wseq *WarnSmsErrQuery) Offset(offset int) *WarnSmsErrQuery {
	wseq.ctx.Offset = &offset
	return wseq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wseq *WarnSmsErrQuery) Unique(unique bool) *WarnSmsErrQuery {
	wseq.ctx.Unique = &unique
	return wseq
}

// Order specifies how the records should be ordered.
func (wseq *WarnSmsErrQuery) Order(o ...warnsmserr.OrderOption) *WarnSmsErrQuery {
	wseq.order = append(wseq.order, o...)
	return wseq
}

// First returns the first WarnSmsErr entity from the query.
// Returns a *NotFoundError when no WarnSmsErr was found.
func (wseq *WarnSmsErrQuery) First(ctx context.Context) (*WarnSmsErr, error) {
	nodes, err := wseq.Limit(1).All(setContextOp(ctx, wseq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{warnsmserr.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) FirstX(ctx context.Context) *WarnSmsErr {
	node, err := wseq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WarnSmsErr ID from the query.
// Returns a *NotFoundError when no WarnSmsErr ID was found.
func (wseq *WarnSmsErrQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = wseq.Limit(1).IDs(setContextOp(ctx, wseq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{warnsmserr.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := wseq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WarnSmsErr entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WarnSmsErr entity is found.
// Returns a *NotFoundError when no WarnSmsErr entities are found.
func (wseq *WarnSmsErrQuery) Only(ctx context.Context) (*WarnSmsErr, error) {
	nodes, err := wseq.Limit(2).All(setContextOp(ctx, wseq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{warnsmserr.Label}
	default:
		return nil, &NotSingularError{warnsmserr.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) OnlyX(ctx context.Context) *WarnSmsErr {
	node, err := wseq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WarnSmsErr ID in the query.
// Returns a *NotSingularError when more than one WarnSmsErr ID is found.
// Returns a *NotFoundError when no entities are found.
func (wseq *WarnSmsErrQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = wseq.Limit(2).IDs(setContextOp(ctx, wseq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{warnsmserr.Label}
	default:
		err = &NotSingularError{warnsmserr.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := wseq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WarnSmsErrs.
func (wseq *WarnSmsErrQuery) All(ctx context.Context) ([]*WarnSmsErr, error) {
	ctx = setContextOp(ctx, wseq.ctx, "All")
	if err := wseq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WarnSmsErr, *WarnSmsErrQuery]()
	return withInterceptors[[]*WarnSmsErr](ctx, wseq, qr, wseq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) AllX(ctx context.Context) []*WarnSmsErr {
	nodes, err := wseq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WarnSmsErr IDs.
func (wseq *WarnSmsErrQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if wseq.ctx.Unique == nil && wseq.path != nil {
		wseq.Unique(true)
	}
	ctx = setContextOp(ctx, wseq.ctx, "IDs")
	if err = wseq.Select(warnsmserr.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := wseq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wseq *WarnSmsErrQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wseq.ctx, "Count")
	if err := wseq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wseq, querierCount[*WarnSmsErrQuery](), wseq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) CountX(ctx context.Context) int {
	count, err := wseq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wseq *WarnSmsErrQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wseq.ctx, "Exist")
	switch _, err := wseq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wseq *WarnSmsErrQuery) ExistX(ctx context.Context) bool {
	exist, err := wseq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WarnSmsErrQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wseq *WarnSmsErrQuery) Clone() *WarnSmsErrQuery {
	if wseq == nil {
		return nil
	}
	return &WarnSmsErrQuery{
		config:     wseq.config,
		ctx:        wseq.ctx.Clone(),
		order:      append([]warnsmserr.OrderOption{}, wseq.order...),
		inters:     append([]Interceptor{}, wseq.inters...),
		predicates: append([]predicate.WarnSmsErr{}, wseq.predicates...),
		// clone intermediate query.
		sql:  wseq.sql.Clone(),
		path: wseq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WarnSmsErr.Query().
//		GroupBy(warnsmserr.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wseq *WarnSmsErrQuery) GroupBy(field string, fields ...string) *WarnSmsErrGroupBy {
	wseq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WarnSmsErrGroupBy{build: wseq}
	grbuild.flds = &wseq.ctx.Fields
	grbuild.label = warnsmserr.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WarnSmsErr.Query().
//		Select(warnsmserr.FieldCreatedAt).
//		Scan(ctx, &v)
func (wseq *WarnSmsErrQuery) Select(fields ...string) *WarnSmsErrSelect {
	wseq.ctx.Fields = append(wseq.ctx.Fields, fields...)
	sbuild := &WarnSmsErrSelect{WarnSmsErrQuery: wseq}
	sbuild.label = warnsmserr.Label
	sbuild.flds, sbuild.scan = &wseq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WarnSmsErrSelect configured with the given aggregations.
func (wseq *WarnSmsErrQuery) Aggregate(fns ...AggregateFunc) *WarnSmsErrSelect {
	return wseq.Select().Aggregate(fns...)
}

func (wseq *WarnSmsErrQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wseq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wseq); err != nil {
				return err
			}
		}
	}
	for _, f := range wseq.ctx.Fields {
		if !warnsmserr.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wseq.path != nil {
		prev, err := wseq.path(ctx)
		if err != nil {
			return err
		}
		wseq.sql = prev
	}
	return nil
}

func (wseq *WarnSmsErrQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WarnSmsErr, error) {
	var (
		nodes = []*WarnSmsErr{}
		_spec = wseq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WarnSmsErr).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WarnSmsErr{config: wseq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(wseq.modifiers) > 0 {
		_spec.Modifiers = wseq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wseq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wseq *WarnSmsErrQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wseq.querySpec()
	if len(wseq.modifiers) > 0 {
		_spec.Modifiers = wseq.modifiers
	}
	_spec.Node.Columns = wseq.ctx.Fields
	if len(wseq.ctx.Fields) > 0 {
		_spec.Unique = wseq.ctx.Unique != nil && *wseq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wseq.driver, _spec)
}

func (wseq *WarnSmsErrQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(warnsmserr.Table, warnsmserr.Columns, sqlgraph.NewFieldSpec(warnsmserr.FieldID, field.TypeUint64))
	_spec.From = wseq.sql
	if unique := wseq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wseq.path != nil {
		_spec.Unique = true
	}
	if fields := wseq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, warnsmserr.FieldID)
		for i := range fields {
			if fields[i] != warnsmserr.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wseq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wseq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wseq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wseq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wseq *WarnSmsErrQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wseq.driver.Dialect())
	t1 := builder.Table(warnsmserr.Table)
	columns := wseq.ctx.Fields
	if len(columns) == 0 {
		columns = warnsmserr.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wseq.sql != nil {
		selector = wseq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wseq.ctx.Unique != nil && *wseq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range wseq.modifiers {
		m(selector)
	}
	for _, p := range wseq.predicates {
		p(selector)
	}
	for _, p := range wseq.order {
		p(selector)
	}
	if offset := wseq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wseq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (wseq *WarnSmsErrQuery) ForUpdate(opts ...sql.LockOption) *WarnSmsErrQuery {
	if wseq.driver.Dialect() == dialect.Postgres {
		wseq.Unique(false)
	}
	wseq.modifiers = append(wseq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return wseq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (wseq *WarnSmsErrQuery) ForShare(opts ...sql.LockOption) *WarnSmsErrQuery {
	if wseq.driver.Dialect() == dialect.Postgres {
		wseq.Unique(false)
	}
	wseq.modifiers = append(wseq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return wseq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (wseq *WarnSmsErrQuery) Modify(modifiers ...func(s *sql.Selector)) *WarnSmsErrSelect {
	wseq.modifiers = append(wseq.modifiers, modifiers...)
	return wseq.Select()
}

// WarnSmsErrGroupBy is the group-by builder for WarnSmsErr entities.
type WarnSmsErrGroupBy struct {
	selector
	build *WarnSmsErrQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wsegb *WarnSmsErrGroupBy) Aggregate(fns ...AggregateFunc) *WarnSmsErrGroupBy {
	wsegb.fns = append(wsegb.fns, fns...)
	return wsegb
}

// Scan applies the selector query and scans the result into the given value.
func (wsegb *WarnSmsErrGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wsegb.build.ctx, "GroupBy")
	if err := wsegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WarnSmsErrQuery, *WarnSmsErrGroupBy](ctx, wsegb.build, wsegb, wsegb.build.inters, v)
}

func (wsegb *WarnSmsErrGroupBy) sqlScan(ctx context.Context, root *WarnSmsErrQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wsegb.fns))
	for _, fn := range wsegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wsegb.flds)+len(wsegb.fns))
		for _, f := range *wsegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wsegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wsegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WarnSmsErrSelect is the builder for selecting fields of WarnSmsErr entities.
type WarnSmsErrSelect struct {
	*WarnSmsErrQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wses *WarnSmsErrSelect) Aggregate(fns ...AggregateFunc) *WarnSmsErrSelect {
	wses.fns = append(wses.fns, fns...)
	return wses
}

// Scan applies the selector query and scans the result into the given value.
func (wses *WarnSmsErrSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wses.ctx, "Select")
	if err := wses.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WarnSmsErrQuery, *WarnSmsErrSelect](ctx, wses.WarnSmsErrQuery, wses, wses.inters, v)
}

func (wses *WarnSmsErrSelect) sqlScan(ctx context.Context, root *WarnSmsErrQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wses.fns))
	for _, fn := range wses.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wses.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wses.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (wses *WarnSmsErrSelect) Modify(modifiers ...func(s *sql.Selector)) *WarnSmsErrSelect {
	wses.modifiers = append(wses.modifiers, modifiers...)
	return wses
}
