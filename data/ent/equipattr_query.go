// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/blues120/ias-core/data/ent/equipattr"
	"github.com/blues120/ias-core/data/ent/predicate"
)

// EquipAttrQuery is the builder for querying EquipAttr entities.
type EquipAttrQuery struct {
	config
	ctx        *QueryContext
	order      []equipattr.OrderOption
	inters     []Interceptor
	predicates []predicate.EquipAttr
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EquipAttrQuery builder.
func (eaq *EquipAttrQuery) Where(ps ...predicate.EquipAttr) *EquipAttrQuery {
	eaq.predicates = append(eaq.predicates, ps...)
	return eaq
}

// Limit the number of records to be returned by this query.
func (eaq *EquipAttrQuery) Limit(limit int) *EquipAttrQuery {
	eaq.ctx.Limit = &limit
	return eaq
}

// Offset to start from.
func (eaq *EquipAttrQuery) Offset(offset int) *EquipAttrQuery {
	eaq.ctx.Offset = &offset
	return eaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eaq *EquipAttrQuery) Unique(unique bool) *EquipAttrQuery {
	eaq.ctx.Unique = &unique
	return eaq
}

// Order specifies how the records should be ordered.
func (eaq *EquipAttrQuery) Order(o ...equipattr.OrderOption) *EquipAttrQuery {
	eaq.order = append(eaq.order, o...)
	return eaq
}

// First returns the first EquipAttr entity from the query.
// Returns a *NotFoundError when no EquipAttr was found.
func (eaq *EquipAttrQuery) First(ctx context.Context) (*EquipAttr, error) {
	nodes, err := eaq.Limit(1).All(setContextOp(ctx, eaq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{equipattr.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eaq *EquipAttrQuery) FirstX(ctx context.Context) *EquipAttr {
	node, err := eaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EquipAttr ID from the query.
// Returns a *NotFoundError when no EquipAttr ID was found.
func (eaq *EquipAttrQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eaq.Limit(1).IDs(setContextOp(ctx, eaq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{equipattr.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eaq *EquipAttrQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := eaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EquipAttr entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EquipAttr entity is found.
// Returns a *NotFoundError when no EquipAttr entities are found.
func (eaq *EquipAttrQuery) Only(ctx context.Context) (*EquipAttr, error) {
	nodes, err := eaq.Limit(2).All(setContextOp(ctx, eaq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{equipattr.Label}
	default:
		return nil, &NotSingularError{equipattr.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eaq *EquipAttrQuery) OnlyX(ctx context.Context) *EquipAttr {
	node, err := eaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EquipAttr ID in the query.
// Returns a *NotSingularError when more than one EquipAttr ID is found.
// Returns a *NotFoundError when no entities are found.
func (eaq *EquipAttrQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eaq.Limit(2).IDs(setContextOp(ctx, eaq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{equipattr.Label}
	default:
		err = &NotSingularError{equipattr.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eaq *EquipAttrQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := eaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EquipAttrs.
func (eaq *EquipAttrQuery) All(ctx context.Context) ([]*EquipAttr, error) {
	ctx = setContextOp(ctx, eaq.ctx, "All")
	if err := eaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EquipAttr, *EquipAttrQuery]()
	return withInterceptors[[]*EquipAttr](ctx, eaq, qr, eaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eaq *EquipAttrQuery) AllX(ctx context.Context) []*EquipAttr {
	nodes, err := eaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EquipAttr IDs.
func (eaq *EquipAttrQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if eaq.ctx.Unique == nil && eaq.path != nil {
		eaq.Unique(true)
	}
	ctx = setContextOp(ctx, eaq.ctx, "IDs")
	if err = eaq.Select(equipattr.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eaq *EquipAttrQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := eaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eaq *EquipAttrQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eaq.ctx, "Count")
	if err := eaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eaq, querierCount[*EquipAttrQuery](), eaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eaq *EquipAttrQuery) CountX(ctx context.Context) int {
	count, err := eaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eaq *EquipAttrQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eaq.ctx, "Exist")
	switch _, err := eaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eaq *EquipAttrQuery) ExistX(ctx context.Context) bool {
	exist, err := eaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EquipAttrQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eaq *EquipAttrQuery) Clone() *EquipAttrQuery {
	if eaq == nil {
		return nil
	}
	return &EquipAttrQuery{
		config:     eaq.config,
		ctx:        eaq.ctx.Clone(),
		order:      append([]equipattr.OrderOption{}, eaq.order...),
		inters:     append([]Interceptor{}, eaq.inters...),
		predicates: append([]predicate.EquipAttr{}, eaq.predicates...),
		// clone intermediate query.
		sql:  eaq.sql.Clone(),
		path: eaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EquipAttr.Query().
//		GroupBy(equipattr.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eaq *EquipAttrQuery) GroupBy(field string, fields ...string) *EquipAttrGroupBy {
	eaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EquipAttrGroupBy{build: eaq}
	grbuild.flds = &eaq.ctx.Fields
	grbuild.label = equipattr.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EquipAttr.Query().
//		Select(equipattr.FieldCreatedAt).
//		Scan(ctx, &v)
func (eaq *EquipAttrQuery) Select(fields ...string) *EquipAttrSelect {
	eaq.ctx.Fields = append(eaq.ctx.Fields, fields...)
	sbuild := &EquipAttrSelect{EquipAttrQuery: eaq}
	sbuild.label = equipattr.Label
	sbuild.flds, sbuild.scan = &eaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EquipAttrSelect configured with the given aggregations.
func (eaq *EquipAttrQuery) Aggregate(fns ...AggregateFunc) *EquipAttrSelect {
	return eaq.Select().Aggregate(fns...)
}

func (eaq *EquipAttrQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eaq); err != nil {
				return err
			}
		}
	}
	for _, f := range eaq.ctx.Fields {
		if !equipattr.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eaq.path != nil {
		prev, err := eaq.path(ctx)
		if err != nil {
			return err
		}
		eaq.sql = prev
	}
	return nil
}

func (eaq *EquipAttrQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EquipAttr, error) {
	var (
		nodes = []*EquipAttr{}
		_spec = eaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EquipAttr).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EquipAttr{config: eaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(eaq.modifiers) > 0 {
		_spec.Modifiers = eaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eaq *EquipAttrQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eaq.querySpec()
	if len(eaq.modifiers) > 0 {
		_spec.Modifiers = eaq.modifiers
	}
	_spec.Node.Columns = eaq.ctx.Fields
	if len(eaq.ctx.Fields) > 0 {
		_spec.Unique = eaq.ctx.Unique != nil && *eaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eaq.driver, _spec)
}

func (eaq *EquipAttrQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(equipattr.Table, equipattr.Columns, sqlgraph.NewFieldSpec(equipattr.FieldID, field.TypeUint64))
	_spec.From = eaq.sql
	if unique := eaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eaq.path != nil {
		_spec.Unique = true
	}
	if fields := eaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, equipattr.FieldID)
		for i := range fields {
			if fields[i] != equipattr.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eaq *EquipAttrQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eaq.driver.Dialect())
	t1 := builder.Table(equipattr.Table)
	columns := eaq.ctx.Fields
	if len(columns) == 0 {
		columns = equipattr.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eaq.sql != nil {
		selector = eaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eaq.ctx.Unique != nil && *eaq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range eaq.modifiers {
		m(selector)
	}
	for _, p := range eaq.predicates {
		p(selector)
	}
	for _, p := range eaq.order {
		p(selector)
	}
	if offset := eaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (eaq *EquipAttrQuery) ForUpdate(opts ...sql.LockOption) *EquipAttrQuery {
	if eaq.driver.Dialect() == dialect.Postgres {
		eaq.Unique(false)
	}
	eaq.modifiers = append(eaq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return eaq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (eaq *EquipAttrQuery) ForShare(opts ...sql.LockOption) *EquipAttrQuery {
	if eaq.driver.Dialect() == dialect.Postgres {
		eaq.Unique(false)
	}
	eaq.modifiers = append(eaq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return eaq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eaq *EquipAttrQuery) Modify(modifiers ...func(s *sql.Selector)) *EquipAttrSelect {
	eaq.modifiers = append(eaq.modifiers, modifiers...)
	return eaq.Select()
}

// EquipAttrGroupBy is the group-by builder for EquipAttr entities.
type EquipAttrGroupBy struct {
	selector
	build *EquipAttrQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eagb *EquipAttrGroupBy) Aggregate(fns ...AggregateFunc) *EquipAttrGroupBy {
	eagb.fns = append(eagb.fns, fns...)
	return eagb
}

// Scan applies the selector query and scans the result into the given value.
func (eagb *EquipAttrGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eagb.build.ctx, "GroupBy")
	if err := eagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipAttrQuery, *EquipAttrGroupBy](ctx, eagb.build, eagb, eagb.build.inters, v)
}

func (eagb *EquipAttrGroupBy) sqlScan(ctx context.Context, root *EquipAttrQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eagb.fns))
	for _, fn := range eagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eagb.flds)+len(eagb.fns))
		for _, f := range *eagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EquipAttrSelect is the builder for selecting fields of EquipAttr entities.
type EquipAttrSelect struct {
	*EquipAttrQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eas *EquipAttrSelect) Aggregate(fns ...AggregateFunc) *EquipAttrSelect {
	eas.fns = append(eas.fns, fns...)
	return eas
}

// Scan applies the selector query and scans the result into the given value.
func (eas *EquipAttrSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eas.ctx, "Select")
	if err := eas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipAttrQuery, *EquipAttrSelect](ctx, eas.EquipAttrQuery, eas, eas.inters, v)
}

func (eas *EquipAttrSelect) sqlScan(ctx context.Context, root *EquipAttrQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eas.fns))
	for _, fn := range eas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eas *EquipAttrSelect) Modify(modifiers ...func(s *sql.Selector)) *EquipAttrSelect {
	eas.modifiers = append(eas.modifiers, modifiers...)
	return eas
}
