// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/predicate"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/tasklimits"
)

// TaskLimitsQuery is the builder for querying TaskLimits entities.
type TaskLimitsQuery struct {
	config
	ctx        *QueryContext
	order      []tasklimits.OrderOption
	inters     []Interceptor
	predicates []predicate.TaskLimits
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TaskLimitsQuery builder.
func (tlq *TaskLimitsQuery) Where(ps ...predicate.TaskLimits) *TaskLimitsQuery {
	tlq.predicates = append(tlq.predicates, ps...)
	return tlq
}

// Limit the number of records to be returned by this query.
func (tlq *TaskLimitsQuery) Limit(limit int) *TaskLimitsQuery {
	tlq.ctx.Limit = &limit
	return tlq
}

// Offset to start from.
func (tlq *TaskLimitsQuery) Offset(offset int) *TaskLimitsQuery {
	tlq.ctx.Offset = &offset
	return tlq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tlq *TaskLimitsQuery) Unique(unique bool) *TaskLimitsQuery {
	tlq.ctx.Unique = &unique
	return tlq
}

// Order specifies how the records should be ordered.
func (tlq *TaskLimitsQuery) Order(o ...tasklimits.OrderOption) *TaskLimitsQuery {
	tlq.order = append(tlq.order, o...)
	return tlq
}

// First returns the first TaskLimits entity from the query.
// Returns a *NotFoundError when no TaskLimits was found.
func (tlq *TaskLimitsQuery) First(ctx context.Context) (*TaskLimits, error) {
	nodes, err := tlq.Limit(1).All(setContextOp(ctx, tlq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tasklimits.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tlq *TaskLimitsQuery) FirstX(ctx context.Context) *TaskLimits {
	node, err := tlq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TaskLimits ID from the query.
// Returns a *NotFoundError when no TaskLimits ID was found.
func (tlq *TaskLimitsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = tlq.Limit(1).IDs(setContextOp(ctx, tlq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tasklimits.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tlq *TaskLimitsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := tlq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TaskLimits entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TaskLimits entity is found.
// Returns a *NotFoundError when no TaskLimits entities are found.
func (tlq *TaskLimitsQuery) Only(ctx context.Context) (*TaskLimits, error) {
	nodes, err := tlq.Limit(2).All(setContextOp(ctx, tlq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tasklimits.Label}
	default:
		return nil, &NotSingularError{tasklimits.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tlq *TaskLimitsQuery) OnlyX(ctx context.Context) *TaskLimits {
	node, err := tlq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TaskLimits ID in the query.
// Returns a *NotSingularError when more than one TaskLimits ID is found.
// Returns a *NotFoundError when no entities are found.
func (tlq *TaskLimitsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = tlq.Limit(2).IDs(setContextOp(ctx, tlq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tasklimits.Label}
	default:
		err = &NotSingularError{tasklimits.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tlq *TaskLimitsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := tlq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TaskLimitsSlice.
func (tlq *TaskLimitsQuery) All(ctx context.Context) ([]*TaskLimits, error) {
	ctx = setContextOp(ctx, tlq.ctx, "All")
	if err := tlq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TaskLimits, *TaskLimitsQuery]()
	return withInterceptors[[]*TaskLimits](ctx, tlq, qr, tlq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tlq *TaskLimitsQuery) AllX(ctx context.Context) []*TaskLimits {
	nodes, err := tlq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TaskLimits IDs.
func (tlq *TaskLimitsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if tlq.ctx.Unique == nil && tlq.path != nil {
		tlq.Unique(true)
	}
	ctx = setContextOp(ctx, tlq.ctx, "IDs")
	if err = tlq.Select(tasklimits.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tlq *TaskLimitsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := tlq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tlq *TaskLimitsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tlq.ctx, "Count")
	if err := tlq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tlq, querierCount[*TaskLimitsQuery](), tlq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tlq *TaskLimitsQuery) CountX(ctx context.Context) int {
	count, err := tlq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tlq *TaskLimitsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tlq.ctx, "Exist")
	switch _, err := tlq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tlq *TaskLimitsQuery) ExistX(ctx context.Context) bool {
	exist, err := tlq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TaskLimitsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tlq *TaskLimitsQuery) Clone() *TaskLimitsQuery {
	if tlq == nil {
		return nil
	}
	return &TaskLimitsQuery{
		config:     tlq.config,
		ctx:        tlq.ctx.Clone(),
		order:      append([]tasklimits.OrderOption{}, tlq.order...),
		inters:     append([]Interceptor{}, tlq.inters...),
		predicates: append([]predicate.TaskLimits{}, tlq.predicates...),
		// clone intermediate query.
		sql:  tlq.sql.Clone(),
		path: tlq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TaskLimits.Query().
//		GroupBy(tasklimits.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tlq *TaskLimitsQuery) GroupBy(field string, fields ...string) *TaskLimitsGroupBy {
	tlq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TaskLimitsGroupBy{build: tlq}
	grbuild.flds = &tlq.ctx.Fields
	grbuild.label = tasklimits.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TaskLimits.Query().
//		Select(tasklimits.FieldCreatedAt).
//		Scan(ctx, &v)
func (tlq *TaskLimitsQuery) Select(fields ...string) *TaskLimitsSelect {
	tlq.ctx.Fields = append(tlq.ctx.Fields, fields...)
	sbuild := &TaskLimitsSelect{TaskLimitsQuery: tlq}
	sbuild.label = tasklimits.Label
	sbuild.flds, sbuild.scan = &tlq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TaskLimitsSelect configured with the given aggregations.
func (tlq *TaskLimitsQuery) Aggregate(fns ...AggregateFunc) *TaskLimitsSelect {
	return tlq.Select().Aggregate(fns...)
}

func (tlq *TaskLimitsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tlq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tlq); err != nil {
				return err
			}
		}
	}
	for _, f := range tlq.ctx.Fields {
		if !tasklimits.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tlq.path != nil {
		prev, err := tlq.path(ctx)
		if err != nil {
			return err
		}
		tlq.sql = prev
	}
	return nil
}

func (tlq *TaskLimitsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TaskLimits, error) {
	var (
		nodes = []*TaskLimits{}
		_spec = tlq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TaskLimits).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TaskLimits{config: tlq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tlq.modifiers) > 0 {
		_spec.Modifiers = tlq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tlq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tlq *TaskLimitsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tlq.querySpec()
	if len(tlq.modifiers) > 0 {
		_spec.Modifiers = tlq.modifiers
	}
	_spec.Node.Columns = tlq.ctx.Fields
	if len(tlq.ctx.Fields) > 0 {
		_spec.Unique = tlq.ctx.Unique != nil && *tlq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tlq.driver, _spec)
}

func (tlq *TaskLimitsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tasklimits.Table, tasklimits.Columns, sqlgraph.NewFieldSpec(tasklimits.FieldID, field.TypeUint64))
	_spec.From = tlq.sql
	if unique := tlq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tlq.path != nil {
		_spec.Unique = true
	}
	if fields := tlq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tasklimits.FieldID)
		for i := range fields {
			if fields[i] != tasklimits.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tlq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tlq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tlq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tlq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tlq *TaskLimitsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tlq.driver.Dialect())
	t1 := builder.Table(tasklimits.Table)
	columns := tlq.ctx.Fields
	if len(columns) == 0 {
		columns = tasklimits.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tlq.sql != nil {
		selector = tlq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tlq.ctx.Unique != nil && *tlq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range tlq.modifiers {
		m(selector)
	}
	for _, p := range tlq.predicates {
		p(selector)
	}
	for _, p := range tlq.order {
		p(selector)
	}
	if offset := tlq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tlq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tlq *TaskLimitsQuery) ForUpdate(opts ...sql.LockOption) *TaskLimitsQuery {
	if tlq.driver.Dialect() == dialect.Postgres {
		tlq.Unique(false)
	}
	tlq.modifiers = append(tlq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tlq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tlq *TaskLimitsQuery) ForShare(opts ...sql.LockOption) *TaskLimitsQuery {
	if tlq.driver.Dialect() == dialect.Postgres {
		tlq.Unique(false)
	}
	tlq.modifiers = append(tlq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tlq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tlq *TaskLimitsQuery) Modify(modifiers ...func(s *sql.Selector)) *TaskLimitsSelect {
	tlq.modifiers = append(tlq.modifiers, modifiers...)
	return tlq.Select()
}

// TaskLimitsGroupBy is the group-by builder for TaskLimits entities.
type TaskLimitsGroupBy struct {
	selector
	build *TaskLimitsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tlgb *TaskLimitsGroupBy) Aggregate(fns ...AggregateFunc) *TaskLimitsGroupBy {
	tlgb.fns = append(tlgb.fns, fns...)
	return tlgb
}

// Scan applies the selector query and scans the result into the given value.
func (tlgb *TaskLimitsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tlgb.build.ctx, "GroupBy")
	if err := tlgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TaskLimitsQuery, *TaskLimitsGroupBy](ctx, tlgb.build, tlgb, tlgb.build.inters, v)
}

func (tlgb *TaskLimitsGroupBy) sqlScan(ctx context.Context, root *TaskLimitsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tlgb.fns))
	for _, fn := range tlgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tlgb.flds)+len(tlgb.fns))
		for _, f := range *tlgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tlgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tlgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TaskLimitsSelect is the builder for selecting fields of TaskLimits entities.
type TaskLimitsSelect struct {
	*TaskLimitsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tls *TaskLimitsSelect) Aggregate(fns ...AggregateFunc) *TaskLimitsSelect {
	tls.fns = append(tls.fns, fns...)
	return tls
}

// Scan applies the selector query and scans the result into the given value.
func (tls *TaskLimitsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tls.ctx, "Select")
	if err := tls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TaskLimitsQuery, *TaskLimitsSelect](ctx, tls.TaskLimitsQuery, tls, tls.inters, v)
}

func (tls *TaskLimitsSelect) sqlScan(ctx context.Context, root *TaskLimitsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tls.fns))
	for _, fn := range tls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tls *TaskLimitsSelect) Modify(modifiers ...func(s *sql.Selector)) *TaskLimitsSelect {
	tls.modifiers = append(tls.modifiers, modifiers...)
	return tls
}
