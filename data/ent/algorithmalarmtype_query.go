// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/algorithmalarmtype"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/predicate"
)

// AlgorithmAlarmTypeQuery is the builder for querying AlgorithmAlarmType entities.
type AlgorithmAlarmTypeQuery struct {
	config
	ctx        *QueryContext
	order      []algorithmalarmtype.OrderOption
	inters     []Interceptor
	predicates []predicate.AlgorithmAlarmType
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AlgorithmAlarmTypeQuery builder.
func (aatq *AlgorithmAlarmTypeQuery) Where(ps ...predicate.AlgorithmAlarmType) *AlgorithmAlarmTypeQuery {
	aatq.predicates = append(aatq.predicates, ps...)
	return aatq
}

// Limit the number of records to be returned by this query.
func (aatq *AlgorithmAlarmTypeQuery) Limit(limit int) *AlgorithmAlarmTypeQuery {
	aatq.ctx.Limit = &limit
	return aatq
}

// Offset to start from.
func (aatq *AlgorithmAlarmTypeQuery) Offset(offset int) *AlgorithmAlarmTypeQuery {
	aatq.ctx.Offset = &offset
	return aatq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aatq *AlgorithmAlarmTypeQuery) Unique(unique bool) *AlgorithmAlarmTypeQuery {
	aatq.ctx.Unique = &unique
	return aatq
}

// Order specifies how the records should be ordered.
func (aatq *AlgorithmAlarmTypeQuery) Order(o ...algorithmalarmtype.OrderOption) *AlgorithmAlarmTypeQuery {
	aatq.order = append(aatq.order, o...)
	return aatq
}

// First returns the first AlgorithmAlarmType entity from the query.
// Returns a *NotFoundError when no AlgorithmAlarmType was found.
func (aatq *AlgorithmAlarmTypeQuery) First(ctx context.Context) (*AlgorithmAlarmType, error) {
	nodes, err := aatq.Limit(1).All(setContextOp(ctx, aatq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{algorithmalarmtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) FirstX(ctx context.Context) *AlgorithmAlarmType {
	node, err := aatq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AlgorithmAlarmType ID from the query.
// Returns a *NotFoundError when no AlgorithmAlarmType ID was found.
func (aatq *AlgorithmAlarmTypeQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aatq.Limit(1).IDs(setContextOp(ctx, aatq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{algorithmalarmtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := aatq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AlgorithmAlarmType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AlgorithmAlarmType entity is found.
// Returns a *NotFoundError when no AlgorithmAlarmType entities are found.
func (aatq *AlgorithmAlarmTypeQuery) Only(ctx context.Context) (*AlgorithmAlarmType, error) {
	nodes, err := aatq.Limit(2).All(setContextOp(ctx, aatq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{algorithmalarmtype.Label}
	default:
		return nil, &NotSingularError{algorithmalarmtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) OnlyX(ctx context.Context) *AlgorithmAlarmType {
	node, err := aatq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AlgorithmAlarmType ID in the query.
// Returns a *NotSingularError when more than one AlgorithmAlarmType ID is found.
// Returns a *NotFoundError when no entities are found.
func (aatq *AlgorithmAlarmTypeQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aatq.Limit(2).IDs(setContextOp(ctx, aatq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{algorithmalarmtype.Label}
	default:
		err = &NotSingularError{algorithmalarmtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := aatq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AlgorithmAlarmTypes.
func (aatq *AlgorithmAlarmTypeQuery) All(ctx context.Context) ([]*AlgorithmAlarmType, error) {
	ctx = setContextOp(ctx, aatq.ctx, "All")
	if err := aatq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AlgorithmAlarmType, *AlgorithmAlarmTypeQuery]()
	return withInterceptors[[]*AlgorithmAlarmType](ctx, aatq, qr, aatq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) AllX(ctx context.Context) []*AlgorithmAlarmType {
	nodes, err := aatq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AlgorithmAlarmType IDs.
func (aatq *AlgorithmAlarmTypeQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if aatq.ctx.Unique == nil && aatq.path != nil {
		aatq.Unique(true)
	}
	ctx = setContextOp(ctx, aatq.ctx, "IDs")
	if err = aatq.Select(algorithmalarmtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := aatq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aatq *AlgorithmAlarmTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aatq.ctx, "Count")
	if err := aatq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aatq, querierCount[*AlgorithmAlarmTypeQuery](), aatq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) CountX(ctx context.Context) int {
	count, err := aatq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aatq *AlgorithmAlarmTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aatq.ctx, "Exist")
	switch _, err := aatq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aatq *AlgorithmAlarmTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := aatq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AlgorithmAlarmTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aatq *AlgorithmAlarmTypeQuery) Clone() *AlgorithmAlarmTypeQuery {
	if aatq == nil {
		return nil
	}
	return &AlgorithmAlarmTypeQuery{
		config:     aatq.config,
		ctx:        aatq.ctx.Clone(),
		order:      append([]algorithmalarmtype.OrderOption{}, aatq.order...),
		inters:     append([]Interceptor{}, aatq.inters...),
		predicates: append([]predicate.AlgorithmAlarmType{}, aatq.predicates...),
		// clone intermediate query.
		sql:  aatq.sql.Clone(),
		path: aatq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AlgorithmAlarmType.Query().
//		GroupBy(algorithmalarmtype.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aatq *AlgorithmAlarmTypeQuery) GroupBy(field string, fields ...string) *AlgorithmAlarmTypeGroupBy {
	aatq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AlgorithmAlarmTypeGroupBy{build: aatq}
	grbuild.flds = &aatq.ctx.Fields
	grbuild.label = algorithmalarmtype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AlgorithmAlarmType.Query().
//		Select(algorithmalarmtype.FieldCreatedAt).
//		Scan(ctx, &v)
func (aatq *AlgorithmAlarmTypeQuery) Select(fields ...string) *AlgorithmAlarmTypeSelect {
	aatq.ctx.Fields = append(aatq.ctx.Fields, fields...)
	sbuild := &AlgorithmAlarmTypeSelect{AlgorithmAlarmTypeQuery: aatq}
	sbuild.label = algorithmalarmtype.Label
	sbuild.flds, sbuild.scan = &aatq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AlgorithmAlarmTypeSelect configured with the given aggregations.
func (aatq *AlgorithmAlarmTypeQuery) Aggregate(fns ...AggregateFunc) *AlgorithmAlarmTypeSelect {
	return aatq.Select().Aggregate(fns...)
}

func (aatq *AlgorithmAlarmTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aatq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aatq); err != nil {
				return err
			}
		}
	}
	for _, f := range aatq.ctx.Fields {
		if !algorithmalarmtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aatq.path != nil {
		prev, err := aatq.path(ctx)
		if err != nil {
			return err
		}
		aatq.sql = prev
	}
	return nil
}

func (aatq *AlgorithmAlarmTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AlgorithmAlarmType, error) {
	var (
		nodes = []*AlgorithmAlarmType{}
		_spec = aatq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AlgorithmAlarmType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AlgorithmAlarmType{config: aatq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(aatq.modifiers) > 0 {
		_spec.Modifiers = aatq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aatq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aatq *AlgorithmAlarmTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aatq.querySpec()
	if len(aatq.modifiers) > 0 {
		_spec.Modifiers = aatq.modifiers
	}
	_spec.Node.Columns = aatq.ctx.Fields
	if len(aatq.ctx.Fields) > 0 {
		_spec.Unique = aatq.ctx.Unique != nil && *aatq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aatq.driver, _spec)
}

func (aatq *AlgorithmAlarmTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(algorithmalarmtype.Table, algorithmalarmtype.Columns, sqlgraph.NewFieldSpec(algorithmalarmtype.FieldID, field.TypeUint64))
	_spec.From = aatq.sql
	if unique := aatq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aatq.path != nil {
		_spec.Unique = true
	}
	if fields := aatq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, algorithmalarmtype.FieldID)
		for i := range fields {
			if fields[i] != algorithmalarmtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aatq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aatq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aatq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aatq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aatq *AlgorithmAlarmTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aatq.driver.Dialect())
	t1 := builder.Table(algorithmalarmtype.Table)
	columns := aatq.ctx.Fields
	if len(columns) == 0 {
		columns = algorithmalarmtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aatq.sql != nil {
		selector = aatq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aatq.ctx.Unique != nil && *aatq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range aatq.modifiers {
		m(selector)
	}
	for _, p := range aatq.predicates {
		p(selector)
	}
	for _, p := range aatq.order {
		p(selector)
	}
	if offset := aatq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aatq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (aatq *AlgorithmAlarmTypeQuery) ForUpdate(opts ...sql.LockOption) *AlgorithmAlarmTypeQuery {
	if aatq.driver.Dialect() == dialect.Postgres {
		aatq.Unique(false)
	}
	aatq.modifiers = append(aatq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return aatq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (aatq *AlgorithmAlarmTypeQuery) ForShare(opts ...sql.LockOption) *AlgorithmAlarmTypeQuery {
	if aatq.driver.Dialect() == dialect.Postgres {
		aatq.Unique(false)
	}
	aatq.modifiers = append(aatq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return aatq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aatq *AlgorithmAlarmTypeQuery) Modify(modifiers ...func(s *sql.Selector)) *AlgorithmAlarmTypeSelect {
	aatq.modifiers = append(aatq.modifiers, modifiers...)
	return aatq.Select()
}

// AlgorithmAlarmTypeGroupBy is the group-by builder for AlgorithmAlarmType entities.
type AlgorithmAlarmTypeGroupBy struct {
	selector
	build *AlgorithmAlarmTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aatgb *AlgorithmAlarmTypeGroupBy) Aggregate(fns ...AggregateFunc) *AlgorithmAlarmTypeGroupBy {
	aatgb.fns = append(aatgb.fns, fns...)
	return aatgb
}

// Scan applies the selector query and scans the result into the given value.
func (aatgb *AlgorithmAlarmTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aatgb.build.ctx, "GroupBy")
	if err := aatgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlgorithmAlarmTypeQuery, *AlgorithmAlarmTypeGroupBy](ctx, aatgb.build, aatgb, aatgb.build.inters, v)
}

func (aatgb *AlgorithmAlarmTypeGroupBy) sqlScan(ctx context.Context, root *AlgorithmAlarmTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aatgb.fns))
	for _, fn := range aatgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aatgb.flds)+len(aatgb.fns))
		for _, f := range *aatgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aatgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aatgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AlgorithmAlarmTypeSelect is the builder for selecting fields of AlgorithmAlarmType entities.
type AlgorithmAlarmTypeSelect struct {
	*AlgorithmAlarmTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aats *AlgorithmAlarmTypeSelect) Aggregate(fns ...AggregateFunc) *AlgorithmAlarmTypeSelect {
	aats.fns = append(aats.fns, fns...)
	return aats
}

// Scan applies the selector query and scans the result into the given value.
func (aats *AlgorithmAlarmTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aats.ctx, "Select")
	if err := aats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlgorithmAlarmTypeQuery, *AlgorithmAlarmTypeSelect](ctx, aats.AlgorithmAlarmTypeQuery, aats, aats.inters, v)
}

func (aats *AlgorithmAlarmTypeSelect) sqlScan(ctx context.Context, root *AlgorithmAlarmTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aats.fns))
	for _, fn := range aats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aats *AlgorithmAlarmTypeSelect) Modify(modifiers ...func(s *sql.Selector)) *AlgorithmAlarmTypeSelect {
	aats.modifiers = append(aats.modifiers, modifiers...)
	return aats
}
