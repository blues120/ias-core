// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/blues120/ias-core/data/ent/predicate"
	"github.com/blues120/ias-core/data/ent/warningtype"
)

// WarningTypeQuery is the builder for querying WarningType entities.
type WarningTypeQuery struct {
	config
	ctx        *QueryContext
	order      []warningtype.OrderOption
	inters     []Interceptor
	predicates []predicate.WarningType
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WarningTypeQuery builder.
func (wtq *WarningTypeQuery) Where(ps ...predicate.WarningType) *WarningTypeQuery {
	wtq.predicates = append(wtq.predicates, ps...)
	return wtq
}

// Limit the number of records to be returned by this query.
func (wtq *WarningTypeQuery) Limit(limit int) *WarningTypeQuery {
	wtq.ctx.Limit = &limit
	return wtq
}

// Offset to start from.
func (wtq *WarningTypeQuery) Offset(offset int) *WarningTypeQuery {
	wtq.ctx.Offset = &offset
	return wtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wtq *WarningTypeQuery) Unique(unique bool) *WarningTypeQuery {
	wtq.ctx.Unique = &unique
	return wtq
}

// Order specifies how the records should be ordered.
func (wtq *WarningTypeQuery) Order(o ...warningtype.OrderOption) *WarningTypeQuery {
	wtq.order = append(wtq.order, o...)
	return wtq
}

// First returns the first WarningType entity from the query.
// Returns a *NotFoundError when no WarningType was found.
func (wtq *WarningTypeQuery) First(ctx context.Context) (*WarningType, error) {
	nodes, err := wtq.Limit(1).All(setContextOp(ctx, wtq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{warningtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wtq *WarningTypeQuery) FirstX(ctx context.Context) *WarningType {
	node, err := wtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WarningType ID from the query.
// Returns a *NotFoundError when no WarningType ID was found.
func (wtq *WarningTypeQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = wtq.Limit(1).IDs(setContextOp(ctx, wtq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{warningtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wtq *WarningTypeQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := wtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WarningType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WarningType entity is found.
// Returns a *NotFoundError when no WarningType entities are found.
func (wtq *WarningTypeQuery) Only(ctx context.Context) (*WarningType, error) {
	nodes, err := wtq.Limit(2).All(setContextOp(ctx, wtq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{warningtype.Label}
	default:
		return nil, &NotSingularError{warningtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wtq *WarningTypeQuery) OnlyX(ctx context.Context) *WarningType {
	node, err := wtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WarningType ID in the query.
// Returns a *NotSingularError when more than one WarningType ID is found.
// Returns a *NotFoundError when no entities are found.
func (wtq *WarningTypeQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = wtq.Limit(2).IDs(setContextOp(ctx, wtq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{warningtype.Label}
	default:
		err = &NotSingularError{warningtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wtq *WarningTypeQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := wtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WarningTypes.
func (wtq *WarningTypeQuery) All(ctx context.Context) ([]*WarningType, error) {
	ctx = setContextOp(ctx, wtq.ctx, "All")
	if err := wtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WarningType, *WarningTypeQuery]()
	return withInterceptors[[]*WarningType](ctx, wtq, qr, wtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wtq *WarningTypeQuery) AllX(ctx context.Context) []*WarningType {
	nodes, err := wtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WarningType IDs.
func (wtq *WarningTypeQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if wtq.ctx.Unique == nil && wtq.path != nil {
		wtq.Unique(true)
	}
	ctx = setContextOp(ctx, wtq.ctx, "IDs")
	if err = wtq.Select(warningtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wtq *WarningTypeQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := wtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wtq *WarningTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wtq.ctx, "Count")
	if err := wtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wtq, querierCount[*WarningTypeQuery](), wtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wtq *WarningTypeQuery) CountX(ctx context.Context) int {
	count, err := wtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wtq *WarningTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wtq.ctx, "Exist")
	switch _, err := wtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wtq *WarningTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := wtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WarningTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wtq *WarningTypeQuery) Clone() *WarningTypeQuery {
	if wtq == nil {
		return nil
	}
	return &WarningTypeQuery{
		config:     wtq.config,
		ctx:        wtq.ctx.Clone(),
		order:      append([]warningtype.OrderOption{}, wtq.order...),
		inters:     append([]Interceptor{}, wtq.inters...),
		predicates: append([]predicate.WarningType{}, wtq.predicates...),
		// clone intermediate query.
		sql:  wtq.sql.Clone(),
		path: wtq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WarningType.Query().
//		GroupBy(warningtype.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wtq *WarningTypeQuery) GroupBy(field string, fields ...string) *WarningTypeGroupBy {
	wtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WarningTypeGroupBy{build: wtq}
	grbuild.flds = &wtq.ctx.Fields
	grbuild.label = warningtype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.WarningType.Query().
//		Select(warningtype.FieldCreatedAt).
//		Scan(ctx, &v)
func (wtq *WarningTypeQuery) Select(fields ...string) *WarningTypeSelect {
	wtq.ctx.Fields = append(wtq.ctx.Fields, fields...)
	sbuild := &WarningTypeSelect{WarningTypeQuery: wtq}
	sbuild.label = warningtype.Label
	sbuild.flds, sbuild.scan = &wtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WarningTypeSelect configured with the given aggregations.
func (wtq *WarningTypeQuery) Aggregate(fns ...AggregateFunc) *WarningTypeSelect {
	return wtq.Select().Aggregate(fns...)
}

func (wtq *WarningTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wtq); err != nil {
				return err
			}
		}
	}
	for _, f := range wtq.ctx.Fields {
		if !warningtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wtq.path != nil {
		prev, err := wtq.path(ctx)
		if err != nil {
			return err
		}
		wtq.sql = prev
	}
	return nil
}

func (wtq *WarningTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WarningType, error) {
	var (
		nodes = []*WarningType{}
		_spec = wtq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WarningType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WarningType{config: wtq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(wtq.modifiers) > 0 {
		_spec.Modifiers = wtq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wtq *WarningTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wtq.querySpec()
	if len(wtq.modifiers) > 0 {
		_spec.Modifiers = wtq.modifiers
	}
	_spec.Node.Columns = wtq.ctx.Fields
	if len(wtq.ctx.Fields) > 0 {
		_spec.Unique = wtq.ctx.Unique != nil && *wtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wtq.driver, _spec)
}

func (wtq *WarningTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(warningtype.Table, warningtype.Columns, sqlgraph.NewFieldSpec(warningtype.FieldID, field.TypeUint64))
	_spec.From = wtq.sql
	if unique := wtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wtq.path != nil {
		_spec.Unique = true
	}
	if fields := wtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, warningtype.FieldID)
		for i := range fields {
			if fields[i] != warningtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wtq *WarningTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wtq.driver.Dialect())
	t1 := builder.Table(warningtype.Table)
	columns := wtq.ctx.Fields
	if len(columns) == 0 {
		columns = warningtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wtq.sql != nil {
		selector = wtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wtq.ctx.Unique != nil && *wtq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range wtq.modifiers {
		m(selector)
	}
	for _, p := range wtq.predicates {
		p(selector)
	}
	for _, p := range wtq.order {
		p(selector)
	}
	if offset := wtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (wtq *WarningTypeQuery) ForUpdate(opts ...sql.LockOption) *WarningTypeQuery {
	if wtq.driver.Dialect() == dialect.Postgres {
		wtq.Unique(false)
	}
	wtq.modifiers = append(wtq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return wtq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (wtq *WarningTypeQuery) ForShare(opts ...sql.LockOption) *WarningTypeQuery {
	if wtq.driver.Dialect() == dialect.Postgres {
		wtq.Unique(false)
	}
	wtq.modifiers = append(wtq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return wtq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (wtq *WarningTypeQuery) Modify(modifiers ...func(s *sql.Selector)) *WarningTypeSelect {
	wtq.modifiers = append(wtq.modifiers, modifiers...)
	return wtq.Select()
}

// WarningTypeGroupBy is the group-by builder for WarningType entities.
type WarningTypeGroupBy struct {
	selector
	build *WarningTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wtgb *WarningTypeGroupBy) Aggregate(fns ...AggregateFunc) *WarningTypeGroupBy {
	wtgb.fns = append(wtgb.fns, fns...)
	return wtgb
}

// Scan applies the selector query and scans the result into the given value.
func (wtgb *WarningTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wtgb.build.ctx, "GroupBy")
	if err := wtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WarningTypeQuery, *WarningTypeGroupBy](ctx, wtgb.build, wtgb, wtgb.build.inters, v)
}

func (wtgb *WarningTypeGroupBy) sqlScan(ctx context.Context, root *WarningTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wtgb.fns))
	for _, fn := range wtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wtgb.flds)+len(wtgb.fns))
		for _, f := range *wtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WarningTypeSelect is the builder for selecting fields of WarningType entities.
type WarningTypeSelect struct {
	*WarningTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wts *WarningTypeSelect) Aggregate(fns ...AggregateFunc) *WarningTypeSelect {
	wts.fns = append(wts.fns, fns...)
	return wts
}

// Scan applies the selector query and scans the result into the given value.
func (wts *WarningTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wts.ctx, "Select")
	if err := wts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WarningTypeQuery, *WarningTypeSelect](ctx, wts.WarningTypeQuery, wts, wts.inters, v)
}

func (wts *WarningTypeSelect) sqlScan(ctx context.Context, root *WarningTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wts.fns))
	for _, fn := range wts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (wts *WarningTypeSelect) Modify(modifiers ...func(s *sql.Selector)) *WarningTypeSelect {
	wts.modifiers = append(wts.modifiers, modifiers...)
	return wts
}
