// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/devicealgo"
	"gitlab.ctyuncdn.cn/ias/ias-core/data/ent/predicate"
)

// DeviceAlgoQuery is the builder for querying DeviceAlgo entities.
type DeviceAlgoQuery struct {
	config
	ctx        *QueryContext
	order      []devicealgo.OrderOption
	inters     []Interceptor
	predicates []predicate.DeviceAlgo
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeviceAlgoQuery builder.
func (daq *DeviceAlgoQuery) Where(ps ...predicate.DeviceAlgo) *DeviceAlgoQuery {
	daq.predicates = append(daq.predicates, ps...)
	return daq
}

// Limit the number of records to be returned by this query.
func (daq *DeviceAlgoQuery) Limit(limit int) *DeviceAlgoQuery {
	daq.ctx.Limit = &limit
	return daq
}

// Offset to start from.
func (daq *DeviceAlgoQuery) Offset(offset int) *DeviceAlgoQuery {
	daq.ctx.Offset = &offset
	return daq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (daq *DeviceAlgoQuery) Unique(unique bool) *DeviceAlgoQuery {
	daq.ctx.Unique = &unique
	return daq
}

// Order specifies how the records should be ordered.
func (daq *DeviceAlgoQuery) Order(o ...devicealgo.OrderOption) *DeviceAlgoQuery {
	daq.order = append(daq.order, o...)
	return daq
}

// First returns the first DeviceAlgo entity from the query.
// Returns a *NotFoundError when no DeviceAlgo was found.
func (daq *DeviceAlgoQuery) First(ctx context.Context) (*DeviceAlgo, error) {
	nodes, err := daq.Limit(1).All(setContextOp(ctx, daq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{devicealgo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (daq *DeviceAlgoQuery) FirstX(ctx context.Context) *DeviceAlgo {
	node, err := daq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeviceAlgo ID from the query.
// Returns a *NotFoundError when no DeviceAlgo ID was found.
func (daq *DeviceAlgoQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = daq.Limit(1).IDs(setContextOp(ctx, daq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{devicealgo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (daq *DeviceAlgoQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := daq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeviceAlgo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeviceAlgo entity is found.
// Returns a *NotFoundError when no DeviceAlgo entities are found.
func (daq *DeviceAlgoQuery) Only(ctx context.Context) (*DeviceAlgo, error) {
	nodes, err := daq.Limit(2).All(setContextOp(ctx, daq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{devicealgo.Label}
	default:
		return nil, &NotSingularError{devicealgo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (daq *DeviceAlgoQuery) OnlyX(ctx context.Context) *DeviceAlgo {
	node, err := daq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeviceAlgo ID in the query.
// Returns a *NotSingularError when more than one DeviceAlgo ID is found.
// Returns a *NotFoundError when no entities are found.
func (daq *DeviceAlgoQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = daq.Limit(2).IDs(setContextOp(ctx, daq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{devicealgo.Label}
	default:
		err = &NotSingularError{devicealgo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (daq *DeviceAlgoQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := daq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeviceAlgos.
func (daq *DeviceAlgoQuery) All(ctx context.Context) ([]*DeviceAlgo, error) {
	ctx = setContextOp(ctx, daq.ctx, "All")
	if err := daq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeviceAlgo, *DeviceAlgoQuery]()
	return withInterceptors[[]*DeviceAlgo](ctx, daq, qr, daq.inters)
}

// AllX is like All, but panics if an error occurs.
func (daq *DeviceAlgoQuery) AllX(ctx context.Context) []*DeviceAlgo {
	nodes, err := daq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeviceAlgo IDs.
func (daq *DeviceAlgoQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if daq.ctx.Unique == nil && daq.path != nil {
		daq.Unique(true)
	}
	ctx = setContextOp(ctx, daq.ctx, "IDs")
	if err = daq.Select(devicealgo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (daq *DeviceAlgoQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := daq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (daq *DeviceAlgoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, daq.ctx, "Count")
	if err := daq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, daq, querierCount[*DeviceAlgoQuery](), daq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (daq *DeviceAlgoQuery) CountX(ctx context.Context) int {
	count, err := daq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (daq *DeviceAlgoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, daq.ctx, "Exist")
	switch _, err := daq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (daq *DeviceAlgoQuery) ExistX(ctx context.Context) bool {
	exist, err := daq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeviceAlgoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (daq *DeviceAlgoQuery) Clone() *DeviceAlgoQuery {
	if daq == nil {
		return nil
	}
	return &DeviceAlgoQuery{
		config:     daq.config,
		ctx:        daq.ctx.Clone(),
		order:      append([]devicealgo.OrderOption{}, daq.order...),
		inters:     append([]Interceptor{}, daq.inters...),
		predicates: append([]predicate.DeviceAlgo{}, daq.predicates...),
		// clone intermediate query.
		sql:  daq.sql.Clone(),
		path: daq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeviceID uint64 `json:"device_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeviceAlgo.Query().
//		GroupBy(devicealgo.FieldDeviceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (daq *DeviceAlgoQuery) GroupBy(field string, fields ...string) *DeviceAlgoGroupBy {
	daq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeviceAlgoGroupBy{build: daq}
	grbuild.flds = &daq.ctx.Fields
	grbuild.label = devicealgo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeviceID uint64 `json:"device_id,omitempty"`
//	}
//
//	client.DeviceAlgo.Query().
//		Select(devicealgo.FieldDeviceID).
//		Scan(ctx, &v)
func (daq *DeviceAlgoQuery) Select(fields ...string) *DeviceAlgoSelect {
	daq.ctx.Fields = append(daq.ctx.Fields, fields...)
	sbuild := &DeviceAlgoSelect{DeviceAlgoQuery: daq}
	sbuild.label = devicealgo.Label
	sbuild.flds, sbuild.scan = &daq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeviceAlgoSelect configured with the given aggregations.
func (daq *DeviceAlgoQuery) Aggregate(fns ...AggregateFunc) *DeviceAlgoSelect {
	return daq.Select().Aggregate(fns...)
}

func (daq *DeviceAlgoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range daq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, daq); err != nil {
				return err
			}
		}
	}
	for _, f := range daq.ctx.Fields {
		if !devicealgo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if daq.path != nil {
		prev, err := daq.path(ctx)
		if err != nil {
			return err
		}
		daq.sql = prev
	}
	return nil
}

func (daq *DeviceAlgoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeviceAlgo, error) {
	var (
		nodes = []*DeviceAlgo{}
		_spec = daq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeviceAlgo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeviceAlgo{config: daq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(daq.modifiers) > 0 {
		_spec.Modifiers = daq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, daq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (daq *DeviceAlgoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := daq.querySpec()
	if len(daq.modifiers) > 0 {
		_spec.Modifiers = daq.modifiers
	}
	_spec.Node.Columns = daq.ctx.Fields
	if len(daq.ctx.Fields) > 0 {
		_spec.Unique = daq.ctx.Unique != nil && *daq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, daq.driver, _spec)
}

func (daq *DeviceAlgoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(devicealgo.Table, devicealgo.Columns, sqlgraph.NewFieldSpec(devicealgo.FieldID, field.TypeUint64))
	_spec.From = daq.sql
	if unique := daq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if daq.path != nil {
		_spec.Unique = true
	}
	if fields := daq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, devicealgo.FieldID)
		for i := range fields {
			if fields[i] != devicealgo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := daq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := daq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := daq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := daq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (daq *DeviceAlgoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(daq.driver.Dialect())
	t1 := builder.Table(devicealgo.Table)
	columns := daq.ctx.Fields
	if len(columns) == 0 {
		columns = devicealgo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if daq.sql != nil {
		selector = daq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if daq.ctx.Unique != nil && *daq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range daq.modifiers {
		m(selector)
	}
	for _, p := range daq.predicates {
		p(selector)
	}
	for _, p := range daq.order {
		p(selector)
	}
	if offset := daq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := daq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (daq *DeviceAlgoQuery) ForUpdate(opts ...sql.LockOption) *DeviceAlgoQuery {
	if daq.driver.Dialect() == dialect.Postgres {
		daq.Unique(false)
	}
	daq.modifiers = append(daq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return daq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (daq *DeviceAlgoQuery) ForShare(opts ...sql.LockOption) *DeviceAlgoQuery {
	if daq.driver.Dialect() == dialect.Postgres {
		daq.Unique(false)
	}
	daq.modifiers = append(daq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return daq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (daq *DeviceAlgoQuery) Modify(modifiers ...func(s *sql.Selector)) *DeviceAlgoSelect {
	daq.modifiers = append(daq.modifiers, modifiers...)
	return daq.Select()
}

// DeviceAlgoGroupBy is the group-by builder for DeviceAlgo entities.
type DeviceAlgoGroupBy struct {
	selector
	build *DeviceAlgoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dagb *DeviceAlgoGroupBy) Aggregate(fns ...AggregateFunc) *DeviceAlgoGroupBy {
	dagb.fns = append(dagb.fns, fns...)
	return dagb
}

// Scan applies the selector query and scans the result into the given value.
func (dagb *DeviceAlgoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dagb.build.ctx, "GroupBy")
	if err := dagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceAlgoQuery, *DeviceAlgoGroupBy](ctx, dagb.build, dagb, dagb.build.inters, v)
}

func (dagb *DeviceAlgoGroupBy) sqlScan(ctx context.Context, root *DeviceAlgoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dagb.fns))
	for _, fn := range dagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dagb.flds)+len(dagb.fns))
		for _, f := range *dagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeviceAlgoSelect is the builder for selecting fields of DeviceAlgo entities.
type DeviceAlgoSelect struct {
	*DeviceAlgoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (das *DeviceAlgoSelect) Aggregate(fns ...AggregateFunc) *DeviceAlgoSelect {
	das.fns = append(das.fns, fns...)
	return das
}

// Scan applies the selector query and scans the result into the given value.
func (das *DeviceAlgoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, das.ctx, "Select")
	if err := das.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceAlgoQuery, *DeviceAlgoSelect](ctx, das.DeviceAlgoQuery, das, das.inters, v)
}

func (das *DeviceAlgoSelect) sqlScan(ctx context.Context, root *DeviceAlgoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(das.fns))
	for _, fn := range das.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*das.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := das.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (das *DeviceAlgoSelect) Modify(modifiers ...func(s *sql.Selector)) *DeviceAlgoSelect {
	das.modifiers = append(das.modifiers, modifiers...)
	return das
}
